# 🚀 VirtApp v3.2 — Immortal Update

`Update by:` 👤 **NIFILIM1337**
`Team:` 🎯 **Naguib's Team**
`Date:` 📅 **07.12.2025**

━━━━━━━━━━━━━━━━━━━━━━━━

## ♾️ IMMORTAL CYCLE (Цикл Бессмертия)
Внедрена архитектура `Rolling Token Strategy` для Epic Games Launcher.

### ⚙️ Алгоритм работы (v7.1):
1️⃣ **Restore:** При старте скрипт восстанавливает "Золотой слепок" из `Documents\Saved`.
2️⃣ **Launch:** Запуск Epic Games в тихом режиме.
3️⃣ **Token Refresh:** Пауза `50 секунд`.
   └ _Зачем:_ `EpicWebHelper` асинхронно сбрасывает кэш на диск. 50 сек гарантируют, что файл `Cookies/webcache` обновился физически.
4️⃣ **Capture:** Принудительное завершение процессов Epic.
5️⃣ **Update:** Перезапись бэкапа в `Documents` новыми файлами.
   └ _Результат:_ Бэкап всегда имеет "свежесть" `0 дней`.

━━━━━━━━━━━━━━━━━━━━━━━━

## 🛡️ Safety & Core Logic
🔹 **No GUI:** Полностью удален код визуального поиска (`pyautogui`, `pillow`). Работа идет только на уровне файловой системы (`shutil`).
🔹 **Game Detection:** Добавлена проверка `is_game_running()`.
   └ _Логика:_ Если `GTA5.exe` или `ragemp_v.exe` запущены — скрипт **не убивает** процессы и не трогает файлы.
🔹 **Full Backup:** Изменена логика копирования. Теперь копируется вся директория `Saved` рекурсивно (`Config` + `webcache` + `Logs` + `Data`).

━━━━━━━━━━━━━━━━━━━━━━━━

## ⚡ System Improvements
🔸 **Time Sync:** Внедрена синхронизация системного времени с `NTP` серверами (критично для SSL).
🔸 **Optimization:** Скрипт очищен от legacy-кода.

━━━━━━━━━━━━━━━━━━━━━━━━

## 📦 Build Details
```
Bot Version:    v3.2
Script Version: epic_auth.py v7.1
Compiler:       MSVC 2022 (Release)
Status:         STABLE
```

_© NIFILIM1337 | Naguib's Team | GTA5RP Automation_