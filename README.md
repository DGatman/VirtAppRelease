# GTA5rpVirt - Automation Bot

Продвинутый бот для автоматизации действий в GTA5RP с управлением через Telegram, OCR-распознаванием и интеграцией с Google Sheets.

## 📋 Содержание

- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Установка](#установка)
- [Настройка](#настройка)
- [Сборка](#сборка)
- [Использование](#использование)
- [Telegram Команды](#telegram-команды)
- [Troubleshooting](#troubleshooting)

## ✨ Возможности

- 🤖 **Telegram Bot** - Полное управление через Telegram
- 🖼️ **OCR** - Распознавание текста на экране (Tesseract)
- 🖱️ **Input Simulation** - Эмуляция клавиатуры и мыши (Interception Driver)
- 📊 **Google Sheets Integration** - Автоматическое обновление статистики
- 🎮 **Game Automation** - Автоматизация рутинных действий (AFK, рулетка) с безопасным выходом через F10 и пропуском цикла без ребута при сбоях
- 📸 **Screenshots** - Удаленный мониторинг через скриншоты

## 🏗️ Архитектура

```
┌─────────────────┐
│  Telegram Bot   │ ◄─── Управление
└────────┬────────┘
         │
    ┌────▼─────┐
    │  C++ App │
    └─┬──┬──┬──┘
      │  │  │
      │  │  └──► Python Scripts ──► Google Sheets API
      │  │
      │  └─────► Tesseract OCR ──► Screen Analysis
      │
      └────────► Interception ──► Game Input
```

### Структура Проекта

```
GTA5rpVirt/
├── src/                  # C++ исходный код
│   ├── main.cpp          # Точка входа, логика бота
│   ├── VirtApp.h         # Заголовочный файл
│   ├── ScreenScaner.cpp  # OCR и захват экрана
│   ├── ScreenScaner.h
│   ├── utils.c           # Вспомогательные функции
│   └── utils.h
├── scripts/              # Python скрипты
│   ├── main.py           # Работа с Google Sheets
│   ├── get_server_from_sheet.py
│   └── ...
├── data/                 # Данные приложения
│   └── tessdata/         # Языковые файлы Tesseract
│       ├── eng.traineddata
│       └── rus.traineddata
├── config/               # Конфигурация
│   └── config.txt        # Настройки бота
├── CMakeLists.txt        # Конфигурация сборки
└── README.md
```

## 📦 Требования

### Системные Требования
- **OS**: Windows 10/11 (x64)
- **RAM**: 4GB+
- **Разрешение**: 1280x720 (для корректной работы координат)

### C++ Зависимости
- **CMake** 3.10.2+
- **Visual Studio 2019+** (MSVC компилятор)
- **vcpkg** - менеджер пакетов для C++

### Библиотеки (через vcpkg)
```bash
vcpkg install tgbot-cpp:x64-windows
vcpkg install tesseract:x64-windows
vcpkg install openssl:x64-windows
vcpkg install boost-system:x64-windows
vcpkg install curl:x64-windows
vcpkg install mailio:x64-windows
vcpkg install sqlite-modern-cpp:x64-windows
```

### Внешние Зависимости
- **[Interception Driver](http://www.oblita.com/interception.html)** - Драйвер для эмуляции ввода
- **Python 3.10+** - Для работы скриптов

### Python Зависимости
```bash
pip install gspread google-auth requests
```

## 🚀 Установка

### 1. Установка vcpkg

```bash
git clone https://github.com/microsoft/vcpkg.git
cd vcpkg
.\bootstrap-vcpkg.bat
.\vcpkg integrate install
```

### 2. Установка Библиотек

```bash
vcpkg install tgbot-cpp tesseract openssl boost-system curl mailio sqlite-modern-cpp --triplet x64-windows
```

### 3. Установка Interception Driver

1. Скачайте с [официального сайта](http://www.oblita.com/interception.html)
2. Распакуйте в удобную директорию (например, `C:\Interception`)
3. Запустите `install-interception.exe` от имени администратора
4. Перезагрузите компьютер

### 4. Клонирование Репозитория

```bash
git clone <repository-url>
cd GTA5rpVirt
```

## ⚙️ Настройка

### 1. Конфигурация CMake

Отредактируйте `CMakeLists.txt`, укажите пути к библиотекам:

```cmake
set(INTERCEPTION_INCLUDE_DIR "C:/Interception/library" CACHE PATH ...)
set(INTERCEPTION_LIB "C:/Interception/library/x64/interception.lib" CACHE FILEPATH ...)
set(TGBOT_LIB "C:/vcpkg/installed/x64-windows/lib/TgBot.lib" CACHE FILEPATH ...)
```

### 2. Конфигурация Бота

Создайте `config/config.txt`:

```ini
PCNAME=YOUR_PC_NAME
Login=your_gta5rp_login
Active Character=1
Email=your_email@example.com
Password=your_password
IMAP=imap.gmail.com
SocialLogin=your_social_club_login
SocialPassword=your_social_club_password
AFK=OFF
```

### 3. Telegram Bot Token

Создайте переменную окружения:

```bash
setx TOKEN "your_telegram_bot_token"
```

Или добавьте в системные переменные через GUI.

### 4. Google Sheets (Опционально)

Если используете интеграцию с Google Sheets:
1. Создайте Service Account в Google Cloud Console
2. Скачайте `credentials.json`
3. Поместите в корень проекта

## 🔨 Сборка

```bash
mkdir build
cd build
cmake .. -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build . --config Release
```

Исполняемый файл будет в `build/Release/VirtApp.exe`

## 🎮 Использование

### Запуск

```bash
cd build/Release
VirtApp.exe
```

При первом запуске бот:
1. Проверит наличие Interception Driver
2. Загрузит Tesseract языковые файлы
3. Подключится к Telegram
4. Отправит сообщение "Verified" администратору

### Остановка

- `Ctrl+C` в консоли
- Команда `/restart` в Telegram (перезагрузит ПК!)

## 📱 Telegram Команды

### Основные
- `/start` - Показать клавиатуру с командами
- `/photo` - Сделать скриншот экрана
- `/photofile` - Скриншот без сжатия (как документ)
- `/photoText` - Скриншот + распознанный текст

### Управление Режимами
- `/afk` - Переключить AFK режим
- `/ruletka` - Переключить режим рулетки
- `/forcerelogin` - Форсировать повторный вход

### Мониторинг
- `/vip` - Проверить VIP статус
- `/bp` - Проверить Battle Pass
- `/dp` - Проверить Donate Points
- `/log` - Получить историю логов
- `/getlogfile` - Скачать logs.txt

### Системные
- `/gpu` - Информация о видеокарте
- `/hwid` - Информация о железе
- `/config` - Показать текущий конфиг
- `/setconfig` - Обновить конфиг
- `/pycheck` - Проверить установку Python
- `/restart` - Перезагрузить ПК (⚠️ осторожно!)

### Дополнительные
- `/profiles` - Получить статистику персонажей
- `/user` - Информация о пользователе
- `/fps` - Показать FPS в игре
- `/cad` - Отправить Ctrl+Alt+Del

## 🔧 Troubleshooting

### Бот не запускается

**Проблема**: `Failed to initialize CURL`
- **Решение**: Убедитесь, что CURL установлен через vcpkg

**Проблема**: `Interception driver not found`
- **Решение**: Установите Interception Driver и перезагрузите ПК

### OCR не работает

**Проблема**: Tesseract не распознает текст
- **Решение**: Проверьте наличие `data/tessdata/rus.traineddata` и `eng.traineddata`

### Python скрипты не выполняются

**Проблема**: `main.py not found`
- **Решение**: Убедитесь, что папка `scripts/` скопирована в `build/Release/`

**Проблема**: `No module named 'gspread'`
- **Решение**: `pip install gspread google-auth requests`

### Telegram команды не работают

**Проблема**: Бот не отвечает
- **Решение**: Проверьте переменную окружения `TOKEN`
- **Решение**: Убедитесь, что ваш Telegram ID добавлен в `tgListSecurity` (в `main.cpp`)

## 📄 Лицензия

Проект для личного использования. Все права защищены.

## ⚠️ Disclaimer

Этот бот предназначен для автоматизации рутинных действий. Использование ботов может нарушать правила игры. Используйте на свой риск.

