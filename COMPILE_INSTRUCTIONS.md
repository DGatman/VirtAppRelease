# 🔧 ИНСТРУКЦИЯ ПО КОМПИЛЯЦИИ ПРОЕКТА

## Текущий статус

✅ **Готовый exe уже скопирован** в папку: `Release_Build/`

Файл: `C:\Users\Nifilim-Server#1\Documents\GTA5rpVirt\Release_Build\VirtApp.exe`

Это полностью рабочая версия с DLL зависимостями.

---

## Для пересборки с новыми изменениями

### Шаг 1: Установить компилятор

**Вариант A: Visual Studio Community (рекомендуется)**
1. Скачать: https://visualstudio.microsoft.com/download/
2. При установке выбрать: "Desktop development with C++"
3. Время: ~30 минут

**Вариант B: MinGW-w64 (компактнее)**
1. Скачать: https://www.mingw-w64.org/
2. Выбрать x86_64 версию
3. Добавить в PATH

### Шаг 2: Открыть проект в VS Code

```bash
cd C:\Users\Nifilim-Server#1\Documents\GTA5rpVirt
code .
```

### Шаг 3: Скомпилировать

**Способ 1: Через CMake Extensions в VS Code**
1. Нажать Ctrl+Shift+P
2. Выполнить: "CMake: Configure"
3. Выбрать компилятор (MSVC или MinGW)
4. Нажать: "CMake: Build"

**Способ 2: Через командную строку**

```bash
# Если установлена VS 2022 с MSVC:
cd Bot
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release
```

### Шаг 4: Найти готовый exe

```
Bot/build/Release/VirtApp.exe
```

---

## 🚨 ВАЖНО: Зависимости (не забыть!)

Эти библиотеки ДОЛЖНЫ быть установлены:

- **Tesseract OCR** - распознавание текста
- **Boost** - утилиты C++
- **OpenSSL** - шифрование
- **CURL** - HTTP запросы
- **TgBot-cpp** - Telegram API
- **mailio** - отправка писем
- **Interception Driver** - перехват ввода

Они уже включены в `.dll` файлы в папке `Release_Build/`, поэтому готовый exe просто копируется вместе с ними.

---

## Текущее окружение

- ✅ CMake: установлен (3.28.0)
- ❌ Visual Studio: НЕ установлена
- ❌ MinGW: НЕ установлен
- ✅ VS Code: установлен
- ✅ CMake Tools расширение: установлено

---

## Быстрый старт

Используйте готовый exe прямо сейчас:

```bash
cd Release_Build
.\VirtApp.exe
```

Он содержит все необходимые DLL зависимости!
