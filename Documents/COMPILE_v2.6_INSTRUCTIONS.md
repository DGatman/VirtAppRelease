# 📋 Инструкция сборки VirtApp v2.6

## ✅ Что уже сделано:
- Исходный код `src/main.cpp` обновлен до версии **v2.6**
- Логика рулетки исправлена:
  - lvl >= 3 → рулетка **ВКЛ автоматически**
  - lvl < 3 → рулетка **ВЫКЛ**
  - lvl >= 5 → AFK **ВЫКЛ**
- Исправлены баги `waitingphone == 0` на `waitingphone = 0`
- Файл проекта готов: `Bot/VirtApp.sln`

## ❌ Проблема:
На вашей системе **НЕТ Visual Studio 2022** (необходимо для компиляции)

## 🔧 Решение - УСТАНОВКА Visual Studio 2022 Community:

### Шаг 1: Скачайте Visual Studio 2022 Community
- Перейдите на: https://visualstudio.microsoft.com/downloads/
- Скачайте **Visual Studio Community 2022**

### Шаг 2: Установка
1. Запустите скачанный installer
2. Выберите компоненты:
   - ✅ Desktop development with C++ (основное)
   - ✅ C++ CMake tools for Windows
   - ✅ C++ core features
3. Установите (~8GB, ~30 минут)

### Шаг 3: Откройте проект
```
C:\Users\Nifilim-Server#1\Documents\GTA5rpVirt\Bot\VirtApp.sln
```

### Шаг 4: Соберите Release версию
1. В Visual Studio выберите конфигурацию: **Release|x64**
2. Нажмите: Build → Build Solution
3. Дождитесь завершения

### Шаг 5: Скопируйте exe
После успешной сборки файл появится в:
```
C:\Users\Nifilim-Server#1\Documents\GTA5rpVirt\Bot\Release\VirtApp.exe
```

---

## 📝 Алтернатива (если нет VS):

Используйте текущий exe из `Release_Build/VirtApp.exe` и:
1. Обновите конфиг `config.txt` вручную
2. Проверьте логику уровня через телеграм команды `/status`
3. После установки VS - пересоберите для полного обновления

---

## 🚀 Версия v2.6 - Что изменилось:

```cpp
// МЕСТО 1: После первого входа (строка 2735-2770)
if (current_lvl >= 3) {
    ruletka = true;          // Автоматически включить
    ruletka_user_disabled = false;  // Сброс флага
} else {
    ruletka = false;         // Отключить
}

if (current_lvl >= max_lvl_int) {
    afk = false;             // Выключить AFK если max уровень
}

// МЕСТО 2: Во время выбора персонажа (строка 2835-2867)
// Идентичная логика
```

---

## ✨ После сборки:
- Новый exe будет в `Bot/Release/VirtApp.exe`
- Скопируйте в рабочую папку
- Запустите и проверьте логику

**Статус:** Код готов к компиляции. Ожидание установки Visual Studio.
