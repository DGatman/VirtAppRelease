# Contributing to GTA5rpVirt

Спасибо за интерес к проекту! Это руководство поможет вам начать разработку.

## 🛠️ Настройка Окружения Разработки

### Требования
- Visual Studio 2019+ с C++ workload
- CMake 3.10.2+
- vcpkg
- Git

### Первоначальная Настройка

1. Клонируйте репозиторий:
```bash
git clone <repo-url>
cd GTA5rpVirt
```

2. Установите зависимости:
```bash
vcpkg install tgbot-cpp tesseract openssl boost-system curl mailio sqlite-modern-cpp --triplet x64-windows
```

3. Настройте CMake:
```bash
mkdir build
cd build
cmake .. -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake
```

## 📝 Стиль Кода

### C++
- Используйте **camelCase** для переменных и функций
- Используйте **PascalCase** для классов
- Отступы: **табы** (как в существующем коде)
- Комментарии на русском языке для бизнес-логики

### Python
- Следуйте **PEP 8**
- Используйте **snake_case** для функций и переменных
- Docstrings для всех публичных функций

## 🌿 Git Workflow

### Ветки
- `main` - стабильная версия
- `develop` - разработка новых фичей
- `feature/<название>` - новые функции
- `bugfix/<название>` - исправления багов

### Коммиты
Используйте понятные сообщения коммитов:
```
feat: добавлена команда /stats
fix: исправлена утечка памяти в ScreenScaner
docs: обновлен README
refactor: вынесены константы в отдельный файл
```

### Pull Requests
1. Создайте ветку от `develop`
2. Внесите изменения
3. Убедитесь, что код компилируется
4. Создайте PR с описанием изменений

## 🧪 Тестирование

Перед коммитом:
1. Убедитесь, что проект компилируется без ошибок
2. Проверьте основные команды бота
3. Проверьте, что OCR работает корректно

## 📚 Архитектура

### Основные Компоненты

**main.cpp** - Точка входа, инициализация бота
- Telegram bot handlers
- Основной игровой цикл
- Управление состоянием

**ScreenScaner** - Работа с экраном
- Захват скриншотов (GDI+)
- OCR распознавание (Tesseract)
- Проверка пикселей

**Python Scripts** - Интеграция с внешними сервисами
- Google Sheets API
- Получение статистики персонажей

### Добавление Новой Команды

1. Добавьте обработчик в `main.cpp`:
```cpp
bot.getEvents().onCommand("mycommand", [&bot](Message::Ptr message) {
    // Проверка прав доступа
    if (find(tgListSecurity.begin(), tgListSecurity.end(), 
             to_string(message->from->id)) == tgListSecurity.end()) {
        bot.getApi().sendMessage(message->chat->id, "No permission");
        return;
    }
    
    // Ваша логика
    bot.getApi().sendMessage(message->chat->id, "Response");
});
```

2. Обновите README с описанием команды

## 🐛 Отладка

### Логирование
Используйте функцию `logprint()` для вывода в консоль и файл:
```cpp
logprint("Debug message", currentTm);
```

### Проверка OCR
Используйте команду `/photoText` для проверки распознавания

### Проверка Input Simulation
Убедитесь, что Interception Driver установлен корректно

## 📞 Контакты

Если у вас есть вопросы, создайте Issue в репозитории.
