# 🚀 VirtApp v2.6 - Готово к компиляции

**Дата:** 01.12.2025  
**Версия:** v2.6  
**Коммит:** 4abf1e7  
**Статус:** ✅ Код готов, нужна компиляция

---

## 📋 Что сделано

- ✅ Исправлена рулетка: автоматическое включение при `lvl >= 3`
- ✅ Исправлена логика AFK: отключается при `lvl >= 5`
- ✅ Фиксы ошибок в коде (waitingphone присваивание)
- ✅ Код загружен на GitHub (репозиторий `RootOne1337/GTA5rpVirt`)

---

## 🔨 Как скомпилировать

### **Вариант 1: Отправить на компиляцию (РЕКОМЕНДУЕТСЯ)**

Отправь ссылку девелоперу / Ilitch'у:

```
🔗 https://github.com/RootOne1337/GTA5rpVirt
📌 Ветка: main (последний коммит с v2.6)
```

**Что ему нужно:**
- Visual Studio 2022
- CMake 3.28+
- vcpkg с пакетами:
  - `openssl:x64-windows`
  - `boost:x64-windows`
  - `tesseract:x64-windows`
  - `curl:x64-windows`
  - `mailio:x64-windows`

**Команда сборки:**
```bash
cd GTA5rpVirt
mkdir build && cd build
cmake -G "Visual Studio 17 2022" -A x64 -DCMAKE_BUILD_TYPE=Release \
  -DCMAKE_TOOLCHAIN_FILE="C:/vcpkg/scripts/buildsystems/vcpkg.cmake" \
  -DVCPKG_TARGET_TRIPLET=x64-windows-release ..
cmake --build . --config Release --parallel 4
```

**Результат:** `build\Release\VirtApp.exe`

---

### **Вариант 2: Компилировать самостоятельно**

1. Установить **Visual Studio 2022 Community**
2. Установить **CMake 3.28+**
3. Установить **vcpkg**
4. Установить все зависимости через vcpkg (долго!)
5. Запустить `BUILD_v2.6_SCRIPT.bat`
6. Ждать 30-60 минут

**Риск:** Может не скомпилироваться из-за конфликтов библиотек

---

### **Вариант 3: Использовать текущий exe**

```
📁 C:\Users\Nifilim-Server#1\Documents\GTA5rpVirt\Release_Build\VirtApp.exe
```

✅ Работает сейчас  
⚠️ Может быть старая версия рулетки

---

## 📝 Изменения в v2.6

### `src/main.cpp`

**Строки 765:** Версия
```cpp
string version = "v2.6";  // Рулетка: lvl>=3 ВКЛ, lvl<3 ВЫКЛ. AFK: lvl>=5 ВЫКЛ
```

**Строки 2735-2770, 2835-2867:** Логика уровня
```cpp
if (current_lvl >= 3) {
    ruletka = true;
    ruletka_user_disabled = false;  // Перегрузить ручное отключение
} else {
    ruletka = false;
}

if (current_lvl >= 5) {  // или max_lvl
    afk = false;
} else {
    afk = true;
}
```

---

## ✨ После компиляции

1. Скопировать `VirtApp.exe` из `build\Release\`
2. Переименовать в `VirtApp_v2.6.exe`
3. Поместить в `Release_Build\`
4. **Запустить и тестировать!**

---

## 📞 Контакты

- 📁 **GitHub:** https://github.com/RootOne1337/GTA5rpVirt
- 💬 **Telegram:** Отправить Ilitch'у или девелоперу
- 🐛 **Issues:** Если что-то не работает

---

## 🎯 Тестирование v2.6

После запуска проверить:

1. **Уровень >= 3:** Рулетка должна **автоматически включиться** (даже если ручно отключал)
2. **Уровень < 3:** Рулетка **выключена**
3. **Уровень >= 5:** AFK **выключен** автоматически
4. **F7 хоткей:** Работает как раньше

---

**Готово к развёртыванию!** 🚀
